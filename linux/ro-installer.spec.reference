# RPM packaging reference

The canonical RPM spec for this project is:

```text
ro-installer.spec
```

This file is intentionally kept as a pointer only. Do not maintain a second
spec copy under `linux/`; duplicate specs drift and hide packaging regressions.
